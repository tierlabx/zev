{
	"name": "zev-web",
	"private": true,
	"scripts": {
		"dev": "pnpm -F admin dev",
		"build": "pnpm -F admin build",
		"lint": "biome check  --write .",
		"format": "biome format --write .",
		"shadcn:add": "pnpm dlx shadcn@latest add -c apps/admin"
	},
	"devDependencies": {
		"@biomejs/biome": "latest",
		"@types/react": "^19.2.14",
		"@types/react-dom": "^19.2.3"
	}
}
